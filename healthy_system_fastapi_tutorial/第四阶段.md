# Healthy System FastAPI 第四阶段详细教程

## 1. 这一阶段到底要解决什么

第四阶段先做一件很重要的事:

把聊天相关的数据结构先稳定下来，也就是先把:

1. `Conversation`
2. `Message`

这两个模块建好。

注意，这一阶段先不要急着接大模型。

因为聊天功能本质上至少有两层:

1. 会话层 `conversation`
2. 消息层 `message`

如果这两层的数据模型不先稳定，后面你一接模型调用，就会不停返工。

所以第四阶段的目标不是“先聊起来”，而是:

先把聊天数据真正存得清楚、查得清楚、关联得清楚。

---

## 2. 第四阶段完成后的验收标准

做到下面这些，就算第四阶段合格:

1. `conversation` 与 `user` 正确关联
2. `message` 与 `conversation` 正确关联
3. 用户可以创建会话
4. 用户可以查看自己的会话列表
5. 用户可以查看某个会话详情
6. 用户可以修改会话标题
7. 用户可以删除会话
8. 用户可以查看某个会话下的历史消息
9. 消息可以手动插入并落库
10. 用户不能查看别人的会话和消息

---

## 3. 为什么这一阶段先做数据结构，不先接大模型

因为你后面真正的聊天接口，本质上一定会依赖这两张表:

1. `conversations`
2. `messages`

你以后无论是:

1. 普通问答
2. 上下文续聊
3. 多轮记忆
4. 历史记录回放
5. 主动消息回显

都离不开这两层数据。

如果你现在不先把它们建稳，后面接大模型时会不断遇到这些问题:

1. 消息不知道该挂到哪个会话下
2. 会话不知道归哪个用户
3. 历史消息查不出来
4. 想补标题、状态、角色时又得返工改表

所以第四阶段最正确的思路是:

先把数据库结构和基础 CRUD 做好，再谈聊天主链路。

---

## 4. 这一阶段先不做什么

这一阶段先不要做这些内容:

1. 不做流式输出
2. 不做 WebSocket
3. 不做复杂 prompt 编排
4. 不做上下文截断策略
5. 不做自动生成会话标题
6. 不做 LLM 调用封装
7. 不做 `/chat` 真正问答接口

这一阶段你只要做到:

1. 用户能创建会话
2. 用户能查历史消息
3. 消息能落库

就已经完成目标了。

---

## 5. 先说明你当前项目里的草稿文件

你现在仓库里已经有这些草稿文件:

1. `backend/app/db/models/conversation.py`
2. `backend/app/db/models/message.py`
3. `backend/app/schemas/conversation.py`
4. `backend/app/schemas/chat.py`

但你前两阶段、第三阶段已经逐步统一成了正式结构:

1. ORM 模型放在 `app/models/`
2. Alembic 通过 `app/db/base.py` 找模型
3. 路由放在 `app/api/routes/`
4. service 放在 `app/services/`

所以第四阶段也要继续统一成这套正式结构。

### 5.1 这一阶段的处理原则

1. 正式版 `Conversation` 放到 `app/models/conversation.py`
2. 正式版 `Message` 放到 `app/models/message.py`
3. `app/db/models/conversation.py` 和 `app/db/models/message.py` 先不要再继续用
4. `app/schemas/conversation.py` 可以直接重写
5. 新增 `app/schemas/message.py`
6. `app/schemas/chat.py` 这一阶段先不要动

### 5.2 为什么这里不继续沿用 `profile_id`

你现在草稿里的 `conversation.py` 用的是 `profile_id`。

但第四阶段的验收标准写得很明确:

`conversation` 与 `user` 正确关联

所以这一阶段推荐直接改成:

`Conversation -> user_id`

这样做好处是:

1. 和第二阶段的认证体系直接对上
2. 当前用户权限判断最简单
3. 这一阶段更容易演示
4. 不依赖 profile 是否已创建

以后如果你要做“一个账号管理多个不同聊天对象”，再往 `profile_id` 扩展也不迟。

---

## 6. 第四阶段推荐的数据设计

## 6.1 Conversation 表

推荐字段:

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int | 主键 |
| `user_id` | int | 归属用户 |
| `title` | str | 会话标题 |
| `status` | str | 会话状态 |
| `created_at` | datetime | 创建时间 |
| `updated_at` | datetime | 更新时间 |

### 为什么这里加 `status`

虽然这一阶段你还不会真的用到很多状态流转，但建议先把 `status` 留好。

因为以后你很可能会需要:

1. 激活中的会话
2. 归档会话
3. 已关闭会话

第四阶段先保留最小状态字段，后面就不容易返工。

推荐先用:

1. `active`
2. `archived`

但这一阶段先只默认写 `active`，不做复杂状态切换接口。

## 6.2 Message 表

推荐字段:

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | int | 主键 |
| `conversation_id` | int | 所属会话 |
| `role` | str | 角色 |
| `content` | text | 消息内容 |
| `message_type` | str | 消息类型 |
| `created_at` | datetime | 创建时间 |

### `role` 推荐值

1. `user`
2. `assistant`
3. `system`

这三个值是聊天系统里最经典、最通用的一套角色设计。

第四阶段先固定成这三个值，后面你接大模型时就不用重构历史消息结构了。

### `message_type` 为什么先保留

虽然这一阶段大多数消息都只是普通文本，但这个字段建议先留好。

后面你可能会扩展:

1. `text`
2. `summary`
3. `event`
4. `reminder`

第四阶段先统一默认成 `text` 就够了。

---

## 7. 这一阶段会改哪些文件

如果你按本教程做，第四阶段会涉及下面这些文件:

```text
backend/
├─ app/
│  ├─ api/
│  │  ├─ routes/
│  │  │  ├─ conversations.py
│  │  │  ├─ messages.py
│  │  │  └─ router.py
│  ├─ models/
│  │  ├─ conversation.py
│  │  ├─ message.py
│  │  └─ __init__.py
│  ├─ schemas/
│  │  ├─ conversation.py
│  │  └─ message.py
│  ├─ services/
│  │  ├─ conversation_service.py
│  │  └─ message_service.py
│  └─ db/
│     └─ base.py
└─ alembic/
   └─ versions/
```

说明:

1. `app/models/conversation.py` 是新建
2. `app/models/message.py` 是新建
3. `app/services/conversation_service.py` 是新建
4. `app/services/message_service.py` 是新建
5. `app/api/routes/conversations.py` 是新建
6. `app/api/routes/messages.py` 是新建
7. `app/schemas/conversation.py` 是重写
8. `app/schemas/message.py` 是新建
9. `app/models/__init__.py`、`app/db/base.py`、`app/api/router.py` 都要补内容

---

## 8. 这一阶段建议提供的接口

## 8.1 Conversation 接口

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| `POST` | `/api/v1/conversations` | 创建会话 |
| `GET` | `/api/v1/conversations` | 获取会话列表 |
| `GET` | `/api/v1/conversations/{conversation_id}` | 获取会话详情 |
| `PUT` | `/api/v1/conversations/{conversation_id}/title` | 修改会话标题 |
| `DELETE` | `/api/v1/conversations/{conversation_id}` | 删除会话 |

## 8.2 Message 接口

| 方法 | 路径 | 作用 |
| --- | --- | --- |
| `GET` | `/api/v1/conversations/{conversation_id}/messages` | 获取某会话消息列表 |
| `POST` | `/api/v1/conversations/{conversation_id}/messages/debug` | 手动插入消息，调试用 |

为什么这里把消息接口设计成挂在会话下面:

因为消息一定属于某个会话。  
所以用嵌套路由最直观:

`conversation -> messages`

---

## 9. 开发顺序建议

这个顺序不要乱:

1. 先做 `Conversation` 模型
2. 再做 `Message` 模型
3. 再补 `app/models/__init__.py`
4. 再补 `app/db/base.py`
5. 再写 `Conversation` 的 schema
6. 再写 `Message` 的 schema
7. 再写 `ConversationService`
8. 再写 `MessageService`
9. 再写 `conversations.py`
10. 再写 `messages.py`
11. 再补总路由 `app/api/router.py`
12. 最后做 Alembic migration 和 Swagger 验收

原因:

1. 没有模型，后面都没法写
2. 没有 schema，请求和响应边界不清楚
3. 没有 service，路由里会堆满业务逻辑
4. 没有 base 导入，Alembic 很可能生成空 migration

---

## 10. 正式开始写代码

下面开始按文件一个一个来。

为了降低你出错概率，这里尽量采用下面这种方式:

1. 明确告诉你这个文件是新建还是替换
2. 给你完整代码
3. 告诉你这份代码在做什么
4. 标明容易写错的地方

---

## 11. 先建 `Conversation` ORM 模型

### 11.1 新建 `app/models/conversation.py`

完整代码如下:

```python
from __future__ import annotations

from datetime import UTC, datetime

from sqlalchemy import DateTime, ForeignKey, Integer, String
from sqlalchemy.orm import Mapped, mapped_column

from app.db.session import Base


class Conversation(Base):
    __tablename__ = "conversations"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    user_id: Mapped[int] = mapped_column(
        ForeignKey("users.id", ondelete="CASCADE"),
        index=True,
        nullable=False,
    )
    title: Mapped[str] = mapped_column(String(100), nullable=False, default="New Conversation")
    status: Mapped[str] = mapped_column(String(20), nullable=False, default="active", index=True)
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        default=lambda: datetime.now(UTC),
        nullable=False,
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        default=lambda: datetime.now(UTC),
        onupdate=lambda: datetime.now(UTC),
        nullable=False,
    )
```

### 11.2 这份模型代码在做什么

1. `user_id` 表示这个会话属于哪个用户
2. `title` 是会话标题
3. `status` 是会话状态
4. `created_at` 是创建时间
5. `updated_at` 是最后更新时间

### 11.3 这里为什么先不加 relationship

这一阶段的核心目标是:

把会话和消息的数据结构先跑通。

所以第四阶段先只用外键表达关联关系就够了。

也就是说:

1. `Conversation.user_id -> users.id`
2. `Message.conversation_id -> conversations.id`

这已经足够支撑 CRUD 和历史消息查询。

后面如果你熟悉了 SQLAlchemy relationship，再补也不迟。

---

## 12. 再建 `Message` ORM 模型

### 12.1 新建 `app/models/message.py`

完整代码如下:

```python
from __future__ import annotations

from datetime import UTC, datetime

from sqlalchemy import DateTime, ForeignKey, Integer, String, Text
from sqlalchemy.orm import Mapped, mapped_column

from app.db.session import Base


class Message(Base):
    __tablename__ = "messages"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, index=True)
    conversation_id: Mapped[int] = mapped_column(
        ForeignKey("conversations.id", ondelete="CASCADE"),
        index=True,
        nullable=False,
    )
    role: Mapped[str] = mapped_column(String(20), nullable=False, index=True)
    content: Mapped[str] = mapped_column(Text, nullable=False)
    message_type: Mapped[str] = mapped_column(String(30), nullable=False, default="text")
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        default=lambda: datetime.now(UTC),
        nullable=False,
        index=True,
    )
```

### 12.2 这份模型代码在做什么

1. `conversation_id` 表示这条消息属于哪个会话
2. `role` 表示消息角色
3. `content` 表示消息内容
4. `message_type` 表示消息类型
5. `created_at` 表示消息写入时间

### 12.3 为什么这里不加 `updated_at`

因为聊天消息一般是追加式数据，不是频繁修改的数据。

第四阶段的最小模型里，消息只需要:

1. 知道是谁说的
2. 知道说了什么
3. 知道什么时候写入

就够了。

---

## 13. 把 `Conversation` 和 `Message` 注册到模型入口

如果你不把模型导入统一入口，Alembic 很可能发现不了它们。

这一步必须做。

### 13.1 修改 `app/models/__init__.py`

如果你已经按第三阶段做了 `Record`，建议这里直接替换成下面这份累积版代码。

完整代码如下:

```python
from app.models.conversation import Conversation
from app.models.message import Message
from app.models.profile import Profile
from app.models.record import Record
from app.models.user import User

__all__ = ["User", "Profile", "Record", "Conversation", "Message"]
```

如果你还没有真正落地第三阶段，那就把上面代码里的 `Record` 那两行先去掉。

### 13.2 修改 `app/db/base.py`

同样建议直接替换成下面这份累积版代码。

完整代码如下:

```python
from app.db.session import Base
from app.models.conversation import Conversation  # noqa: F401
from app.models.message import Message  # noqa: F401
from app.models.profile import Profile  # noqa: F401
from app.models.record import Record  # noqa: F401
from app.models.user import User  # noqa: F401

__all__ = ["Base", "User", "Profile", "Record", "Conversation", "Message"]
```

如果你还没有真正落地第三阶段，那就把 `Record` 那两行先去掉。

### 13.3 这一步为什么非常关键

因为 Alembic 自动生成 migration 时，要通过 `Base.metadata` 扫描模型。

如果你忘了导入:

1. `Conversation`
2. `Message`

那后果通常就是:

1. migration 文件为空
2. 数据库里根本没有这两张表
3. 路由一调用就报错

---

## 14. 写 `Conversation` 的 Schema

schema 要解决的核心问题是:

1. 创建会话时允许前端传什么
2. 改标题时允许前端传什么
3. 读取会话时要返回什么

### 14.1 重写 `app/schemas/conversation.py`

你当前仓库里已经有这个文件，但内容还是很早期的草稿版。  
第四阶段建议直接整文件替换。

完整代码如下:

```python
from datetime import datetime
from enum import Enum

from pydantic import BaseModel, ConfigDict, Field


class ConversationStatus(str, Enum):
    active = "active"
    archived = "archived"


class ConversationCreate(BaseModel):
    title: str | None = Field(default=None, max_length=100)


class ConversationTitleUpdate(BaseModel):
    title: str = Field(..., min_length=1, max_length=100)


class ConversationRead(BaseModel):
    id: int
    user_id: int
    title: str
    status: ConversationStatus
    created_at: datetime
    updated_at: datetime

    model_config = ConfigDict(from_attributes=True)


class ConversationListData(BaseModel):
    items: list[ConversationRead]
```

### 14.2 每个 schema 的作用

#### `ConversationStatus`

作用:

把会话状态限制在固定范围内。

这一阶段先保留两个最小值:

1. `active`
2. `archived`

#### `ConversationCreate`

作用:

创建会话时使用。

这里让标题可不传，是为了让你在第四阶段可以快速创建一个空标题会话。

如果用户没传标题，后端会自动补默认值。

#### `ConversationTitleUpdate`

作用:

专门给“修改会话标题”接口使用。

这里把 `title` 设成必填，是因为这个接口只做一件事:

改标题。

#### `ConversationRead`

作用:

返回给前端的会话读取结构。

#### `ConversationListData`

作用:

统一会话列表的返回数据结构。

---

## 15. 写 `Message` 的 Schema

### 15.1 新建 `app/schemas/message.py`

完整代码如下:

```python
from datetime import datetime
from enum import Enum

from pydantic import BaseModel, ConfigDict, Field


class MessageRole(str, Enum):
    user = "user"
    assistant = "assistant"
    system = "system"


class MessageCreate(BaseModel):
    role: MessageRole
    content: str = Field(..., min_length=1)
    message_type: str = Field(default="text", min_length=1, max_length=30)


class MessageRead(BaseModel):
    id: int
    conversation_id: int
    role: MessageRole
    content: str
    message_type: str
    created_at: datetime

    model_config = ConfigDict(from_attributes=True)


class MessageListData(BaseModel):
    items: list[MessageRead]
```

### 15.2 每个 schema 的作用

#### `MessageRole`

作用:

把消息角色固定成三种:

1. `user`
2. `assistant`
3. `system`

这是后面真正接大模型时最常用的一套消息角色结构。

#### `MessageCreate`

作用:

用于第四阶段“手动插入消息，调试用”的接口。

#### `MessageRead`

作用:

把数据库里的消息返回给前端。

#### `MessageListData`

作用:

统一消息列表返回结构。

---

## 16. 写 `ConversationService`

service 层负责:

1. 会话归属校验
2. 创建会话
3. 查询会话列表
4. 查询会话详情
5. 修改标题
6. 删除会话

### 16.1 新建 `app/services/conversation_service.py`

完整代码如下:

```python
from sqlalchemy import delete, select
from sqlalchemy.orm import Session

from app.core.exception import BusinessException
from app.models.conversation import Conversation
from app.models.message import Message
from app.schemas.conversation import ConversationCreate, ConversationTitleUpdate


class ConversationService:
    def create_for_user(
        self,
        db: Session,
        user_id: int,
        payload: ConversationCreate,
    ) -> Conversation:
        title = (payload.title or "").strip() or "New Conversation"

        conversation = Conversation(
            user_id=user_id,
            title=title,
            status="active",
        )
        db.add(conversation)
        db.commit()
        db.refresh(conversation)
        return conversation

    def list_for_user(
        self,
        db: Session,
        user_id: int,
    ) -> list[Conversation]:
        stmt = (
            select(Conversation)
            .where(Conversation.user_id == user_id)
            .order_by(Conversation.updated_at.desc(), Conversation.id.desc())
        )
        return list(db.scalars(stmt).all())

    def get_or_raise(
        self,
        db: Session,
        user_id: int,
        conversation_id: int,
    ) -> Conversation:
        stmt = select(Conversation).where(
            Conversation.id == conversation_id,
            Conversation.user_id == user_id,
        )
        conversation = db.scalar(stmt)
        if not conversation:
            raise BusinessException(
                code=40431,
                message="conversation not found",
                status_code=404,
            )
        return conversation

    def update_title_for_user(
        self,
        db: Session,
        user_id: int,
        conversation_id: int,
        payload: ConversationTitleUpdate,
    ) -> Conversation:
        conversation = self.get_or_raise(db, user_id, conversation_id)
        title = payload.title.strip()

        if not title:
            raise BusinessException(
                code=40031,
                message="title cannot be empty",
                status_code=400,
            )

        conversation.title = title
        db.add(conversation)
        db.commit()
        db.refresh(conversation)
        return conversation

    def delete_for_user(
        self,
        db: Session,
        user_id: int,
        conversation_id: int,
    ) -> None:
        conversation = self.get_or_raise(db, user_id, conversation_id)

        db.execute(
            delete(Message).where(Message.conversation_id == conversation.id)
        )
        db.delete(conversation)
        db.commit()


conversation_service = ConversationService()
```

### 16.2 这份 service 代码最关键的点

#### 1. 所有查询都必须带 `user_id`

这一阶段最重要的权限原则就是:

会话必须归当前用户所有。

所以不管是详情、改标题还是删除，都不能只写:

```python
db.get(Conversation, conversation_id)
```

因为这样没有做用户归属限制。

正确思路必须是:

```python
where(Conversation.id == conversation_id, Conversation.user_id == user_id)
```

#### 2. 创建会话时，`user_id` 不能来自前端

它必须来自当前登录用户。

也就是说:

1. 前端负责传标题
2. 后端负责把当前用户 id 写进会话

#### 3. 删除会话时，为什么这里手动先删消息

你当前项目默认数据库是 SQLite。  
而 SQLite 的外键级联删除行为，很多初学者环境里并不会默认稳定生效。

所以为了让你在第四阶段少踩坑，这里直接在 service 里手动删:

1. 先删 `messages`
2. 再删 `conversation`

这样逻辑最稳，也最容易理解。

---

## 17. 写 `MessageService`

`MessageService` 主要负责:

1. 验证会话是否属于当前用户
2. 查询某个会话下的消息列表
3. 手动插入消息用于调试

### 17.1 新建 `app/services/message_service.py`

完整代码如下:

```python
from datetime import UTC, datetime

from sqlalchemy import select
from sqlalchemy.orm import Session

from app.core.exception import BusinessException
from app.models.message import Message
from app.schemas.message import MessageCreate
from app.services.conversation_service import conversation_service


class MessageService:
    def list_for_conversation(
        self,
        db: Session,
        user_id: int,
        conversation_id: int,
    ) -> list[Message]:
        conversation_service.get_or_raise(db, user_id, conversation_id)

        stmt = (
            select(Message)
            .where(Message.conversation_id == conversation_id)
            .order_by(Message.created_at.asc(), Message.id.asc())
        )
        return list(db.scalars(stmt).all())

    def create_debug_for_conversation(
        self,
        db: Session,
        user_id: int,
        conversation_id: int,
        payload: MessageCreate,
    ) -> Message:
        conversation = conversation_service.get_or_raise(
            db, user_id, conversation_id
        )

        content = payload.content.strip()
        if not content:
            raise BusinessException(
                code=40041,
                message="content cannot be empty",
                status_code=400,
            )

        message_type = payload.message_type.strip()
        if not message_type:
            raise BusinessException(
                code=40042,
                message="message_type cannot be empty",
                status_code=400,
            )

        message = Message(
            conversation_id=conversation_id,
            role=payload.role.value,
            content=content,
            message_type=message_type,
        )

        db.add(message)

        conversation.updated_at = datetime.now(UTC)
        db.add(conversation)

        db.commit()
        db.refresh(message)
        return message


message_service = MessageService()
```

### 17.2 这份 service 代码最关键的点

#### 1. 先验证会话归属，再查消息

消息本身没有直接挂 `user_id`，所以权限判断不能直接从 `Message` 表做。

正确做法是:

先验证:

1. 这个 `conversation_id` 存在
2. 并且它属于当前用户

然后再去查这个会话下面的消息。

#### 2. 为什么插入消息时要更新 `conversation.updated_at`

这是一个非常容易忽略的点。

如果你新增了一条消息，但不更新会话的 `updated_at`，那会发生什么?

会话列表排序可能就不准确。

比如:

1. 会话 A 昨天创建
2. 会话 B 今天创建
3. 你今天又往会话 A 里新增了一条消息

按业务直觉，会话 A 应该排到更前面。  
所以插入消息时，最好顺手刷新会话的 `updated_at`。

这一步非常重要。

---

## 18. 写 `Conversation` 路由

路由层负责:

1. 接收请求
2. 注入数据库会话
3. 注入当前用户
4. 调用 service
5. 返回统一响应

### 18.1 新建 `app/api/routes/conversations.py`

完整代码如下:

```python
from fastapi import APIRouter, Depends, status
from sqlalchemy.orm import Session

from app.api.deps import get_current_active_user, get_db
from app.core.response import success_response
from app.models.user import User
from app.schemas.common import ApiResponse
from app.schemas.conversation import (
    ConversationCreate,
    ConversationListData,
    ConversationRead,
    ConversationTitleUpdate,
)
from app.services.conversation_service import conversation_service

router = APIRouter(prefix="/conversations", tags=["conversations"])


@router.post(
    "",
    response_model=ApiResponse[ConversationRead],
    status_code=status.HTTP_201_CREATED,
)
def create_conversation(
    payload: ConversationCreate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user),
):
    conversation = conversation_service.create_for_user(
        db, current_user.id, payload
    )
    return success_response(
        data=ConversationRead.model_validate(conversation),
        message="conversation created",
    )


@router.get(
    "",
    response_model=ApiResponse[ConversationListData],
)
def list_conversations(
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user),
):
    conversations = conversation_service.list_for_user(db, current_user.id)
    return success_response(
        data=ConversationListData(
            items=[
                ConversationRead.model_validate(item)
                for item in conversations
            ]
        ),
        message="success",
    )


@router.get(
    "/{conversation_id}",
    response_model=ApiResponse[ConversationRead],
)
def get_conversation_detail(
    conversation_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user),
):
    conversation = conversation_service.get_or_raise(
        db, current_user.id, conversation_id
    )
    return success_response(
        data=ConversationRead.model_validate(conversation),
        message="success",
    )


@router.put(
    "/{conversation_id}/title",
    response_model=ApiResponse[ConversationRead],
)
def update_conversation_title(
    conversation_id: int,
    payload: ConversationTitleUpdate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user),
):
    conversation = conversation_service.update_title_for_user(
        db=db,
        user_id=current_user.id,
        conversation_id=conversation_id,
        payload=payload,
    )
    return success_response(
        data=ConversationRead.model_validate(conversation),
        message="conversation title updated",
    )


@router.delete(
    "/{conversation_id}",
    response_model=ApiResponse[None],
)
def delete_conversation(
    conversation_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user),
):
    conversation_service.delete_for_user(db, current_user.id, conversation_id)
    return success_response(message="conversation deleted")
```

### 18.2 这份路由代码在做什么

#### `POST /conversations`

作用:

创建一个新的会话。

#### `GET /conversations`

作用:

查看当前用户自己的会话列表。

#### `GET /conversations/{conversation_id}`

作用:

查看某个会话详情。

#### `PUT /conversations/{conversation_id}/title`

作用:

只修改会话标题。

为什么不直接用:

```text
PUT /conversations/{conversation_id}
```

因为第四阶段你只做标题更新，不做整会话复杂更新。  
单独拆一个标题更新接口，语义更清楚。

#### `DELETE /conversations/{conversation_id}`

作用:

删除会话。

这一阶段删除是物理删除，不做回收站。

---

## 19. 写 `Message` 路由

### 19.1 新建 `app/api/routes/messages.py`

完整代码如下:

```python
from fastapi import APIRouter, Depends, status
from sqlalchemy.orm import Session

from app.api.deps import get_current_active_user, get_db
from app.core.response import success_response
from app.models.user import User
from app.schemas.common import ApiResponse
from app.schemas.message import MessageCreate, MessageListData, MessageRead
from app.services.message_service import message_service

router = APIRouter(
    prefix="/conversations/{conversation_id}/messages",
    tags=["messages"],
)


@router.get(
    "",
    response_model=ApiResponse[MessageListData],
)
def list_messages(
    conversation_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user),
):
    messages = message_service.list_for_conversation(
        db, current_user.id, conversation_id
    )
    return success_response(
        data=MessageListData(
            items=[MessageRead.model_validate(item) for item in messages]
        ),
        message="success",
    )


@router.post(
    "/debug",
    response_model=ApiResponse[MessageRead],
    status_code=status.HTTP_201_CREATED,
)
def create_message_debug(
    conversation_id: int,
    payload: MessageCreate,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_active_user),
):
    message = message_service.create_debug_for_conversation(
        db=db,
        user_id=current_user.id,
        conversation_id=conversation_id,
        payload=payload,
    )
    return success_response(
        data=MessageRead.model_validate(message),
        message="message created",
    )
```

### 19.2 这份消息路由为什么这样设计

#### 1. 为什么消息接口挂在会话下面

因为消息一定从属于某个会话。

所以:

`/conversations/{conversation_id}/messages`

比单独搞一个:

`/messages?conversation_id=xxx`

更直观，也更不容易绕晕。

#### 2. 为什么手动插入消息接口叫 `/debug`

因为第四阶段你还没真正接 LLM。

所以这个接口的本质是:

手动往数据库里插消息，方便调试历史消息链路。

它不是最终聊天入口，所以路径里明确标个 `/debug` 更合适。

---

## 20. 把新路由挂到总路由里

如果你不把新路由挂进去，这些接口是不会生效的。

### 20.1 修改 `app/api/router.py`

如果你已经按第三阶段做了 `records` 路由，建议这里直接替换成下面这份累积版代码。

完整代码如下:

```python
from fastapi import APIRouter

from app.api.routes.auth import router as auth_router
from app.api.routes.conversations import router as conversations_router
from app.api.routes.health import router as health_router
from app.api.routes.messages import router as messages_router
from app.api.routes.profiles import router as profiles_router
from app.api.routes.records import router as records_router
from app.api.routes.users import router as users_router

api_router = APIRouter()
api_router.include_router(health_router)
api_router.include_router(auth_router)
api_router.include_router(users_router)
api_router.include_router(profiles_router)
api_router.include_router(records_router)
api_router.include_router(conversations_router)
api_router.include_router(messages_router)
```

如果你还没有真正落地第三阶段，那就把里面和 `records_router` 有关的两行先去掉。

### 20.2 这一步的作用

1. 统一收口所有 API 子路由
2. 让 `main.py` 继续只挂一个总路由
3. 让新增的会话和消息接口真正对外生效

---

## 21. 这一阶段哪些文件不用动

下面这些文件第四阶段一般不需要动:

1. `app/api/deps.py`
2. `app/core/security.py`
3. `app/core/response.py`
4. `app/core/exception.py`
5. `app/main.py`
6. `app/api/routes/auth.py`
7. `app/api/routes/users.py`
8. `app/api/routes/profiles.py`
9. `app/schemas/chat.py`

特别注意:

`app/schemas/chat.py` 虽然名字看起来和聊天有关，但这一阶段先不要碰它。

因为第四阶段先做的是聊天数据结构，不是最终聊天接口。

---

## 22. 现在开始做数据库迁移

前面的 Python 代码都准备好之后，才轮到数据库迁移。

### 22.1 先确认这几件事

在跑 Alembic 之前，先确认:

1. `app/models/conversation.py` 已经存在
2. `app/models/message.py` 已经存在
3. `app/models/__init__.py` 已经导入它们
4. `app/db/base.py` 已经导入它们
5. `app/api/router.py` 已经挂载了新路由
6. 没有正式代码继续 import `app/db/models/conversation.py`
7. 没有正式代码继续 import `app/db/models/message.py`

### 22.2 进入后端目录

```powershell
cd D:\vue_fastapi\backend
```

### 22.3 生成 migration

```powershell
alembic revision --autogenerate -m "create conversations and messages"
```

### 22.4 检查生成出来的 migration 文件

你应该重点确认:

1. 有 `conversations` 表
2. 有 `messages` 表
3. `conversations.user_id` 指向 `users.id`
4. `messages.conversation_id` 指向 `conversations.id`
5. 有必要的索引

### 22.5 如果 migration 是空的，通常只有两个原因

1. 你忘了在 `app/db/base.py` 里导入 `Conversation` 和 `Message`
2. 你把主要代码写在了 `app/db/models/`，但正式入口没有扫描到

### 22.6 执行迁移

```powershell
alembic upgrade head
```

执行成功后，数据库里就应该有:

1. `conversations`
2. `messages`

这两张表了。

---

## 23. 启动项目并做 Swagger 验收

### 23.1 启动后端

如果你平时就是这样启动项目，就继续用你原来的方式。

最常见的是:

```powershell
uvicorn app.main:app --reload
```

### 23.2 打开 Swagger

访问:

```text
http://127.0.0.1:8000/docs
```

### 23.3 先完成登录

第四阶段所有会话和消息接口都依赖当前登录用户，所以必须先:

1. 注册
2. 登录
3. 拿到 token
4. 在 Swagger 里点 `Authorize`

---

## 24. 第四阶段的 Swagger 演示顺序

建议你严格按下面顺序来测。

## 24.1 先创建一个会话

`POST /api/v1/conversations`

请求体可以这样写:

```json
{
  "title": "第一次健康咨询"
}
```

也可以直接传空对象:

```json
{}
```

如果你不传标题，后端会自动补:

`New Conversation`

## 24.2 查看会话列表

`GET /api/v1/conversations`

你应该能看到刚刚创建出来的会话。

## 24.3 查看会话详情

`GET /api/v1/conversations/{conversation_id}`

把上一步返回的 `id` 填进去。

## 24.4 修改会话标题

`PUT /api/v1/conversations/{conversation_id}/title`

请求体示例:

```json
{
  "title": "今天的健康咨询记录"
}
```

## 24.5 手动插入一条用户消息

`POST /api/v1/conversations/{conversation_id}/messages/debug`

请求体示例:

```json
{
  "role": "user",
  "content": "我今天有点头晕，血压也偏高。",
  "message_type": "text"
}
```

## 24.6 再手动插入一条助手消息

`POST /api/v1/conversations/{conversation_id}/messages/debug`

请求体示例:

```json
{
  "role": "assistant",
  "content": "建议先复测血压，并注意休息与补水。",
  "message_type": "text"
}
```

## 24.7 查看这个会话下的历史消息

`GET /api/v1/conversations/{conversation_id}/messages`

你应该能看到:

1. 刚插入的用户消息
2. 刚插入的助手消息
3. 顺序按时间正序排列

## 24.8 删除会话

`DELETE /api/v1/conversations/{conversation_id}`

删完后:

1. 再查这个会话详情，应该报 `conversation not found`
2. 再查这个会话的消息列表，也应该无法通过会话归属校验

---

## 25. 这一阶段最容易写错的地方

### 25.1 继续把 `Conversation` 设计成挂 `profile_id`

这是你当前草稿最容易延续下来的问题。

但第四阶段的验收标准要求是:

`conversation` 与 `user` 正确关联

所以这一阶段正式版必须是:

`Conversation.user_id`

### 25.2 查会话时只按 `conversation_id` 查，不按 `user_id` 限制

这是最典型的越权 bug。

错误写法:

```python
conversation = db.get(Conversation, conversation_id)
```

正确思路:

```python
where(Conversation.id == conversation_id, Conversation.user_id == user_id)
```

### 25.3 查消息时直接按 `conversation_id` 查，没有先校验会话归属

消息表本身没有 `user_id`，所以你不能直接假设:

这个 `conversation_id` 一定属于当前用户。

正确流程一定是:

1. 先校验这个会话归当前用户
2. 再查该会话下的消息

### 25.4 插入消息时忘了更新 `conversation.updated_at`

这会导致会话列表排序不符合实际使用顺序。

### 25.5 删除会话时只删会话，不删消息

这样很容易留下脏数据。

第四阶段为了稳定起见，直接在 service 里手动先删消息，再删会话。

### 25.6 现在就急着去写 `/chat`

第四阶段先不要急。

这一阶段的核心任务只是:

1. 会话建好
2. 消息建好
3. 历史消息能查
4. 消息能落库

把这一步做好，后面接大模型才顺。

---

## 26. 第四阶段做完后，你应该真正学会了什么

做完这一阶段，你应该至少能说清楚下面这些问题:

1. 为什么聊天系统至少要拆成 `Conversation` 和 `Message`
2. 为什么会话要直接归属于 `user`
3. 为什么消息要通过 `conversation_id` 关联
4. 为什么消息列表必须先校验会话归属
5. 为什么插入消息时要同步更新会话的 `updated_at`
6. 为什么这一阶段先不急着接大模型

如果这些你都能讲清楚，说明你已经开始真正理解聊天后端的数据基础了。

---

## 27. 第四阶段最终检查清单

- [ ] `app/models/conversation.py` 已创建
- [ ] `app/models/message.py` 已创建
- [ ] `app/models/__init__.py` 已导入 `Conversation` 和 `Message`
- [ ] `app/db/base.py` 已导入 `Conversation` 和 `Message`
- [ ] `app/schemas/conversation.py` 已重写
- [ ] `app/schemas/message.py` 已创建
- [ ] `app/services/conversation_service.py` 已创建
- [ ] `app/services/message_service.py` 已创建
- [ ] `app/api/routes/conversations.py` 已创建
- [ ] `app/api/routes/messages.py` 已创建
- [ ] `app/api/router.py` 已挂载新路由
- [ ] Alembic migration 已生成
- [ ] Alembic migration 已执行
- [ ] Swagger 可创建会话
- [ ] Swagger 可查看会话列表
- [ ] Swagger 可查看会话详情
- [ ] Swagger 可修改会话标题
- [ ] Swagger 可删除会话
- [ ] Swagger 可查看某会话历史消息
- [ ] Swagger 可手动插入消息
- [ ] `conversation` 与 `user` 正确关联
- [ ] `message` 与 `conversation` 正确关联

---

## 28. 这一阶段完成后的意义

第四阶段做完之后，你的聊天系统就第一次有了真正的数据骨架。

这意味着你后面接大模型时，不再是“边聊边想表怎么设计”，而是已经有稳定的数据落点:

1. 先确定会话
2. 再写入用户消息
3. 再写入助手消息
4. 再回放历史消息

这其实就是后面聊天主链路的基础骨架。

所以第四阶段虽然还没有真正调用 LLM，但它非常关键。  
因为后面的聊天功能，几乎都会建立在这一阶段的数据结构之上。
